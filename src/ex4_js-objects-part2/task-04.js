const upperFirstLetter = (str) => str[0].toUpperCase() + str.slice(1);
//   const arr = str.split('');
//   arr[0] = arr[0].toUpperCase();
//   return arr.join('');
// по мне закомментированный понятнее)

module.exports = upperFirstLetter;
